version: "3.9"

# ─────────────────────────────────────────────────────────────────────────────
# E-Kart — Multi-Vendor E-Commerce Platform
# Docker Compose: all 6 backend services + NGINX gateway + PostgreSQL + Frontend
# ─────────────────────────────────────────────────────────────────────────────

networks:
  ecommerce:
    driver: bridge

volumes:
  postgres_data:
  redis_data:

services:
  # ───────────────────────────────────────────────────────────────────────────
  # Shared Infrastructure
  # ───────────────────────────────────────────────────────────────────────────

  postgres:
    image: postgres:15-alpine
    container_name: ecommerce_postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ecommerce
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ecommerce_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - ecommerce
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ───────────────────────────────────────────────────────────────────────────
  # Microservices (Pre-built images from Docker Hub)
  # ───────────────────────────────────────────────────────────────────────────

  user-service:
    image: ${DOCKERHUB_USERNAME}/user-service:latest
    container_name: ecommerce_user_service
    env_file:
      - .env
    environment:
      PORT: "8000"
    ports:
      - "8001:8000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  product-catalog-service:
    image: ${DOCKERHUB_USERNAME}/product-catalog-service:latest
    container_name: ecommerce_product_service
    env_file:
      - .env
    environment:
      PORT: "8002"
    ports:
      - "8002:8002"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  shopping-cart-service:
    image: ${DOCKERHUB_USERNAME}/shopping-cart-service:latest
    container_name: ecommerce_cart_service
    env_file:
      - .env
    environment:
      PORT: "8003"
    ports:
      - "8003:8003"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  order-service:
    image: ${DOCKERHUB_USERNAME}/order-service:latest
    container_name: ecommerce_order_service
    env_file:
      - .env
    environment:
      PORT: "8004"
    ports:
      - "8004:8004"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  payment-service:
    image: ${DOCKERHUB_USERNAME}/payment-service:latest
    container_name: ecommerce_payment_service
    env_file:
      - .env
    environment:
      PORT: "8005"
    ports:
      - "8005:8005"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  notification-service:
    image: ${DOCKERHUB_USERNAME}/notification-service:latest
    container_name: ecommerce_notification_service
    env_file:
      - .env
    environment:
      PORT: "8006"
    ports:
      - "8006:8006"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ecommerce
    restart: always

  # ───────────────────────────────────────────────────────────────────────────
  # Frontend (Next.js)
  # ───────────────────────────────────────────────────────────────────────────

  frontend:
    image: ${DOCKERHUB_USERNAME}/ekart-frontend:latest
    container_name: ecommerce_frontend
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      - api-gateway
    networks:
      - ecommerce
    restart: always

  # ───────────────────────────────────────────────────────────────────────────
  # API Gateway (NGINX)
  # ───────────────────────────────────────────────────────────────────────────

  api-gateway:
    image: ${DOCKERHUB_USERNAME}/api-gateway:latest
    container_name: ecommerce_gateway
    environment:
      PORT: "80"
      USER_SERVICE_URL: "http://user-service:8000"
      PRODUCT_SERVICE_URL: "http://product-catalog-service:8002"
      CART_SERVICE_URL: "http://shopping-cart-service:8003"
      ORDER_SERVICE_URL: "http://order-service:8004"
      PAYMENT_SERVICE_URL: "http://payment-service:8005"
      NOTIFICATION_SERVICE_URL: "http://notification-service:8006"
    ports:
      - "80:80"
    depends_on:
      - user-service
      - product-catalog-service
      - shopping-cart-service
      - order-service
      - payment-service
      - notification-service
    networks:
      - ecommerce
    restart: always

